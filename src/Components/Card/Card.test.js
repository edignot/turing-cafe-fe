import React from 'react'
import { render, fireEvent } from '@testing-library/react'
import Card from './Card'
import '@testing-library/jest-dom'

describe('<Card />', () => {
    test('Doisplays reservation info correctly', () => {
        expect(true).toBeTruthy()
    })
})